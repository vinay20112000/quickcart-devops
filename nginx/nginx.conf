events {}

http {

    upstream quickcart_backend {
        server quickcart-app:3000;
    }

    server {

        listen 80;

        location / {

            proxy_pass http://quickcart_backend;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;

        }

    }
}
